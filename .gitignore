# IDE/Editor Specific
.idea/
.vscode/
*.sublime*

# System Specific
.DS_Store
Thumbs.db
*.swp

# Go Build Artifacts
bin/
*.exe
*.out
*.test
**/coverage.html

# Logs, Temp Files, & Runtime Data
logs/
*.log
*.tmp
*.bak

# GitHub, CI/CD, and Debugging
*.coverage
*.prof
coverage.out

# Python virtual environments
.venv/
venv/
env/

# beads runtime lock. The .beads/.gitignore rule (*.gate.lock*) only covers
# files INSIDE .beads/, but bd writes this one at the repo ROOT, so it leaks
# into git status on every run.
.beads.gate.lock
